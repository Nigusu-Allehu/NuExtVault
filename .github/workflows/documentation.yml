name: Documentation examples

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: documentation-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  NUGET_XMLDOC_MODE: skip

jobs:
  examples:
    name: Documentation examples (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - windows-latest
          - ubuntu-latest
          - macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Cache NuGet packages
        uses: actions/cache@v6
        with:
          path: ~/.nuget/packages
          key: documentation-nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            documentation-nuget-${{ runner.os }}-

      - name: Restore functional tests
        run: dotnet restore tests/NuExtVault.FunctionalTests/NuExtVault.FunctionalTests.csproj

      - name: Build functional tests
        run: >-
          dotnet build tests/NuExtVault.FunctionalTests/NuExtVault.FunctionalTests.csproj
          --no-restore
          --configuration Release
          --warnaserror

      - name: Run documentation examples
        run: >-
          dotnet test tests/NuExtVault.FunctionalTests/NuExtVault.FunctionalTests.csproj
          --no-restore
          --no-build
          --configuration Release
          --filter "FullyQualifiedName~NuExtVault.FunctionalTests.DocumentationContractTests|FullyQualifiedName~NuExtVault.FunctionalTests.DocumentationExampleTests"
          --logger "console;verbosity=normal"
          --logger "trx;LogFilePrefix=documentation-${{ matrix.os }}"
          --results-directory TestResults

      - name: Upload failed test results
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: documentation-test-results-${{ matrix.os }}
          path: TestResults/**/*.trx
          if-no-files-found: warn
          retention-days: 7
