<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <Description>
      Test-only, separately compiled conformance module. It contributes the
      /flavors/index.json endpoint, its operation, its service-index resource, and its
      requested capability through the public extension SDK only. It must never
      reference NuExtVault or an official extension implementation.
    </Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="NuExtVault.FunctionalTests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NuExtVault.Extensions.Sdk\NuExtVault.Extensions.Sdk.csproj" />
  </ItemGroup>

</Project>
