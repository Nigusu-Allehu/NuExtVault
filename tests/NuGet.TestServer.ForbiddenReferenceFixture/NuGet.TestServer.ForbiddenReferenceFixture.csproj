<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Step 20 (trusted third-party in-process loading) tests-first red phase.
    A deliberately non-conformant separately compiled extension module used only
    to prove the not-yet-implemented external extension loader must reject a
    package whose entry assembly references the host, the kernel, or the
    official extensions assembly. This project must never be referenced by
    production code or by the host at runtime.
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NuGet.TestServer.Extensions.Sdk\NuGet.TestServer.Extensions.Sdk.csproj" />
    <ProjectReference Include="..\..\src\NuGet.TestServer.Kernel\NuGet.TestServer.Kernel.csproj" />
    <ProjectReference Include="..\..\src\NuGet.TestServer.Extensions.Official\NuGet.TestServer.Extensions.Official.csproj" />
    <ProjectReference Include="..\..\src\NuGet.TestServer\NuGet.TestServer.csproj" />
  </ItemGroup>

</Project>
